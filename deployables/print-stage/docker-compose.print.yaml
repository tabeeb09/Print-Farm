services:
  website:
    build:
      context: .
    restart: unless-stopped
    env_file:
      - /etc/print/deploy.env
    volumes:
      - ./.local-state:/app/.local-state
    networks:
      default:
      app_edge:
        aliases:
          - print-web

networks:
  app_edge:
    external: true
